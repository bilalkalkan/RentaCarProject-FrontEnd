import { FilterbrandpipePipe } from './filterbrandpipe.pipe';

describe('FilterbrandpipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbrandpipePipe();
    expect(pipe).toBeTruthy();
  });
});
