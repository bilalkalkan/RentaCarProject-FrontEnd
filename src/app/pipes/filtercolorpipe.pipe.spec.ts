import { FiltercolorpipePipe } from './filtercolorpipe.pipe';

describe('FiltercolorpipePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercolorpipePipe();
    expect(pipe).toBeTruthy();
  });
});
